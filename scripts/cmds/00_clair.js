const axios = require('axios');module.exports = {  config: {    name: 'claire',    version: '1.0',    author: 'LiANE @nealianacagara',    role: 0,    category: 'Ai-Chat',    shortDescription: { en: `Claire is Ai` },    longDescription: { en: `Claire is just an AI who makes the most structured and detailed response. Nothing special` },    guide: { en: '{pn}claire [query]' },  },  onStart: async function ({ api, event }) {    try {      const query = args.join(" ");      if (query) {        const processingMessage = await api.sendMessage(`Asking ✨ Claire. Please wait a moment...`, event.threadID);        const response = await axios.get(`https://lianeapi.onrender.com/@LianeAPI_Reworks/api/claire?query=${encodeURIComponent(query)}`);                if (response.data && response.data.message) {          await api.sendMessage({ body: response.data.message.trim() }, event.threadID, event.messageID);          console.log(`Sent ✨ Claire's response to the user`);        } else {          throw new Error(`Invalid or missing response from ✨ Claire API`);        }        await api.unsendMessage(processingMessage.messageID);      }    } catch (error) {      console.error(`❌ | Failed to get ✨ Claire's response: ${error.message}`);      api.sendMessage(`❌ | An error occured. You can try typing your query again or resending it. There might be an issue with the server that's causing the problem, and it might resolve on retrying.`, event.threadID);    }  },};