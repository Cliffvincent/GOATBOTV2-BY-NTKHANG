const axios = require('axios');module.exports = {  config: {    name: 'cyberdetective',    version: '1.0',    author: 'Sleuth',    role: 0,    category: 'Ai-Chat',    shortDescription: { en: `Uncover the answers\n| you seek with CyberDetective, your AI\n partner in solving mysteries!` },    longDescription: { en: `Uncover the answers you seek with CyberDetective, your AI partner in solving mysteries!` },    guide: { en: '{pn}cyberdetective_by_sleuth [query]' },  },  onStart: async function ({ api, event }) {    try {      const query = args.join(" ");      if (query) {        const processingMessage = await api.sendMessage(`Asking CyberDetective. Please wait a moment...`, event.threadID);        const response = await axios.get(`https://lianeapi.onrender.com/@nealianacagara/api/cyberdetective_by_sleuth?query=${encodeURIComponent(query)}`);                if (response.data && response.data.message) {          await api.sendMessage({ body: response.data.message.trim() }, event.threadID, event.messageID);          console.log(`Sent CyberDetective's response to the user`);        } else {          throw new Error(`Invalid or missing response from CyberDetective API`);        }        await api.unsendMessage(processingMessage.messageID);      }    } catch (error) {      console.error(`❌ | Failed to get CyberDetective's response: ${error.message}`);      api.sendMessage(`❌ | An error occured. You can try typing your query again or resending it. There might be an issue with the server that's causing the problem, and it might resolve on retrying.`, event.threadID);    }  },};