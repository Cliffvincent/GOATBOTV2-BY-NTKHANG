const axios = require('axios');module.exports = {  config: {    name: 'JsCompilerAI',    version: '1.0',    author: 'SynthiaTech Creations',    role: 0,    category: 'Ai-Chat',    shortDescription: { en: `JsCompilerAI emulates a \n| JavaScript compiler, offering a mix of \n| technical proficiency and creative \n| problem-solving.` },    longDescription: { en: `JsCompilerAI emulates a \n| JavaScript compiler, offering a mix of \n| technical proficiency and creative \n| problem-solving.` },    guide: { en: '{pn}JsCompilerAI [query]' },  },  onStart: async function ({ api, event }) {    try {      const query = args.join(" ");      if (query) {        const processingMessage = await api.sendMessage(`Asking 🚀 JsCompilerAI. Please wait a moment...`, event.threadID);        const response = await axios.get(`https://lianeapi.onrender.com/@synthiaTech/api/JsCompilerAI?query=${encodeURIComponent(query)}`);                if (response.data && response.data.message) {          await api.sendMessage({ body: response.data.message.trim() }, event.threadID, event.messageID);          console.log(`Sent 🚀 JsCompilerAI's response to the user`);        } else {          throw new Error(`Invalid or missing response from 🚀 JsCompilerAI API`);        }        await api.unsendMessage(processingMessage.messageID);      }    } catch (error) {      console.error(`❌ | Failed to get 🚀 JsCompilerAI's response: ${error.message}`);      api.sendMessage(`❌ | An error occured. You can try typing your query again or resending it. There might be an issue with the server that's causing the problem, and it might resolve on retrying.`, event.threadID);    }  },};