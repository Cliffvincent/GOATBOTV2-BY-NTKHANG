const axios = require('axios');module.exports = {  config: {    name: 'audrey',    version: '1.0',    author: 'Original Idea from: AizerGD',    role: 0,    category: 'Ai-Chat',    shortDescription: { en: `AudreyAI helps people\n│ answering questions that people \n| cannot\n│ understand and teaches you Russian \n| Español` },    longDescription: { en: `AudreyAI helps people answering questions that people cannot understand and teaches you Russian/Español` },    guide: { en: '{pn}audrey [query]' },  },  onStart: async function ({ api, event }) {    try {      const query = args.join(" ");      if (query) {        const processingMessage = await api.sendMessage(`Asking AudreyAI Helper. Please wait a moment...`, event.threadID);        const response = await axios.get(`https://lianeapi.onrender.com/@AizerGD/api/audrey?query=${encodeURIComponent(query)}`);                if (response.data && response.data.message) {          await api.sendMessage({ body: response.data.message.trim() }, event.threadID, event.messageID);          console.log(`Sent AudreyAI Helper's response to the user`);        } else {          throw new Error(`Invalid or missing response from AudreyAI Helper API`);        }        await api.unsendMessage(processingMessage.messageID);      }    } catch (error) {      console.error(`❌ | Failed to get AudreyAI Helper's response: ${error.message}`);      api.sendMessage(`❌ | An error occured. You can try typing your query again or resending it. There might be an issue with the server that's causing the problem, and it might resolve on retrying.`, event.threadID);    }  },};